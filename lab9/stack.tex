}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
}%
}

Pushed stack ['c', 'a'] and preconditions
\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
(clear a)\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
}%
}

Pushed unstack ['b', 'a'] and preconditions
\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
(unstack b a)\\
(on b a)$\wedge$(clear b)$\wedge$(AE )\\
(on b a)\\
(clear b)\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
(unstack b a)\\
(on b a)$\wedge$(clear b)$\wedge$(AE )\\
(on b a)\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
(unstack b a)\\
(on b a)$\wedge$(clear b)$\wedge$(AE )\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
(unstack b a)\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
(clear a)\\
}%
}

Pushed putdown ['b'] and preconditions
\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
(clear a)\\
(putdown b)\\
(hold b)\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
(clear a)\\
(putdown b)\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
(clear a)\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
}%
}

Pushed stack ['b', 'd'] and preconditions
\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(stack b d)\\
(clear b)$\wedge$(clear d)$\wedge$(AE )\\
(clear b)\\
(clear d)\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(stack b d)\\
(clear b)$\wedge$(clear d)$\wedge$(AE )\\
(clear b)\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(stack b d)\\
(clear b)$\wedge$(clear d)$\wedge$(AE )\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(stack b d)\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
}%
}

\item \textbf{Stack:} pop()
\\ \noindent\fbox{%
\parbox{\textwidth}{%
}%
}

}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
(clear a)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
(unstack b a)\\
(on b a)$\wedge$(clear b)$\wedge$(AE )\\
(on b a)\\
(clear b)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
(unstack b a)\\
(on b a)$\wedge$(clear b)$\wedge$(AE )\\
(on b a)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
(unstack b a)\\
(on b a)$\wedge$(clear b)$\wedge$(AE )\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
(unstack b a)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
(clear a)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
(clear a)\\
(putdown b)\\
(hold b)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
(clear a)\\
(putdown b)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
(clear a)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
(clear c)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
(clear c)$\wedge$(clear a)$\wedge$(AE )\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
(stack c a)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(on b d)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(stack b d)\\
(clear b)$\wedge$(clear d)$\wedge$(AE )\\
(clear b)\\
(clear d)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(stack b d)\\
(clear b)$\wedge$(clear d)$\wedge$(AE )\\
(clear b)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(stack b d)\\
(clear b)$\wedge$(clear d)$\wedge$(AE )\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
(stack b d)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
(ontable a)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
(ontable d)\\
}%
}

(ontable d)$\wedge$(ontable a)$\wedge$(on b d)$\wedge$(on c a)\\
}%
}

}%
}

}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(on b c)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(on b c)\\
(stack a b)\\
(clear a)$\wedge$(clear b)$\wedge$(AE )\\
(clear a)\\
(clear b)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(on b c)\\
(stack a b)\\
(clear a)$\wedge$(clear b)$\wedge$(AE )\\
(clear a)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(on b c)\\
(stack a b)\\
(clear a)$\wedge$(clear b)$\wedge$(AE )\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(on b c)\\
(stack a b)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(on b c)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
(clear c)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(unstack a b)\\
(on a b)$\wedge$(clear a)$\wedge$(AE )\\
(on a b)\\
(clear a)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(unstack a b)\\
(on a b)$\wedge$(clear a)$\wedge$(AE )\\
(on a b)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(unstack a b)\\
(on a b)$\wedge$(clear a)$\wedge$(AE )\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(unstack a b)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
(clear c)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
(clear c)\\
(putdown a)\\
(hold a)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
(clear c)\\
(putdown a)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
(clear c)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(stack c d)\\
(clear c)$\wedge$(clear d)$\wedge$(AE )\\
(clear c)\\
(clear d)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(stack c d)\\
(clear c)$\wedge$(clear d)$\wedge$(AE )\\
(clear c)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(stack c d)\\
(clear c)$\wedge$(clear d)$\wedge$(AE )\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(stack c d)\\
(clear c)$\wedge$(clear d)$\wedge$(AE )\\
(unstack b c)\\
(on b c)$\wedge$(clear b)$\wedge$(AE )\\
(on b c)\\
(clear b)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(stack c d)\\
(clear c)$\wedge$(clear d)$\wedge$(AE )\\
(unstack b c)\\
(on b c)$\wedge$(clear b)$\wedge$(AE )\\
(on b c)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(stack c d)\\
(clear c)$\wedge$(clear d)$\wedge$(AE )\\
(unstack b c)\\
(on b c)$\wedge$(clear b)$\wedge$(AE )\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(stack c d)\\
(clear c)$\wedge$(clear d)$\wedge$(AE )\\
(unstack b c)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(stack c d)\\
(clear c)$\wedge$(clear d)$\wedge$(AE )\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(stack c d)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(stack c d)\\
(clear c)$\wedge$(clear d)$\wedge$(AE )\\
(clear c)\\
(clear d)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(stack c d)\\
(clear c)$\wedge$(clear d)$\wedge$(AE )\\
(clear c)\\
(clear d)\\
(putdown b)\\
(hold b)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(stack c d)\\
(clear c)$\wedge$(clear d)$\wedge$(AE )\\
(clear c)\\
(clear d)\\
(putdown b)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(stack c d)\\
(clear c)$\wedge$(clear d)$\wedge$(AE )\\
(clear c)\\
(clear d)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(stack c d)\\
(clear c)$\wedge$(clear d)$\wedge$(AE )\\
(clear c)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(stack c d)\\
(clear c)$\wedge$(clear d)$\wedge$(AE )\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(stack c d)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
}%
}

}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(on b c)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(on b c)\\
(stack a b)\\
(clear a)$\wedge$(clear b)$\wedge$(AE )\\
(clear a)\\
(clear b)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(on b c)\\
(stack a b)\\
(clear a)$\wedge$(clear b)$\wedge$(AE )\\
(clear a)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(on b c)\\
(stack a b)\\
(clear a)$\wedge$(clear b)$\wedge$(AE )\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(on b c)\\
(stack a b)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(on b c)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
(clear c)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(unstack a b)\\
(on a b)$\wedge$(clear a)$\wedge$(AE )\\
(on a b)\\
(clear a)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(unstack a b)\\
(on a b)$\wedge$(clear a)$\wedge$(AE )\\
(on a b)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(unstack a b)\\
(on a b)$\wedge$(clear a)$\wedge$(AE )\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(unstack a b)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
(clear c)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
(clear c)\\
(putdown a)\\
(hold a)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
(clear c)\\
(putdown a)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
(clear c)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(stack b c)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
}%
}

}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(on b c)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(on b c)\\
(stack a b)\\
(clear a)$\wedge$(clear b)$\wedge$(AE )\\
(clear a)\\
(clear b)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(on b c)\\
(stack a b)\\
(clear a)$\wedge$(clear b)$\wedge$(AE )\\
(clear a)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(on b c)\\
(stack a b)\\
(clear a)$\wedge$(clear b)$\wedge$(AE )\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(on b c)\\
(stack a b)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
(on b c)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
(on c d)\\
}%
}

(on c d)$\wedge$(on b c)$\wedge$(on a b)\\
}%
}

}%
}

}%
}

(on b c)$\wedge$(on a b)\\
(on b c)\\
}%
}

(on b c)$\wedge$(on a b)\\
(on b c)\\
(stack a b)\\
(clear a)$\wedge$(clear b)$\wedge$(AE )\\
(clear a)\\
(clear b)\\
}%
}

(on b c)$\wedge$(on a b)\\
(on b c)\\
(stack a b)\\
(clear a)$\wedge$(clear b)$\wedge$(AE )\\
(clear a)\\
}%
}

(on b c)$\wedge$(on a b)\\
(on b c)\\
(stack a b)\\
(clear a)$\wedge$(clear b)$\wedge$(AE )\\
}%
}

(on b c)$\wedge$(on a b)\\
(on b c)\\
(stack a b)\\
}%
}

(on b c)$\wedge$(on a b)\\
(on b c)\\
}%
}

(on b c)$\wedge$(on a b)\\
}%
}

(on b c)$\wedge$(on a b)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
(clear c)\\
}%
}

(on b c)$\wedge$(on a b)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
}%
}

(on b c)$\wedge$(on a b)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
}%
}

(on b c)$\wedge$(on a b)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(unstack a b)\\
(on a b)$\wedge$(clear a)$\wedge$(AE )\\
(on a b)\\
(clear a)\\
}%
}

(on b c)$\wedge$(on a b)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(unstack a b)\\
(on a b)$\wedge$(clear a)$\wedge$(AE )\\
(on a b)\\
}%
}

(on b c)$\wedge$(on a b)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(unstack a b)\\
(on a b)$\wedge$(clear a)$\wedge$(AE )\\
}%
}

(on b c)$\wedge$(on a b)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(unstack a b)\\
}%
}

(on b c)$\wedge$(on a b)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
}%
}

(on b c)$\wedge$(on a b)\\
(stack b c)\\
}%
}

(on b c)$\wedge$(on a b)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
(clear c)\\
}%
}

(on b c)$\wedge$(on a b)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
(clear c)\\
(putdown a)\\
(hold a)\\
}%
}

(on b c)$\wedge$(on a b)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
(clear c)\\
(putdown a)\\
}%
}

(on b c)$\wedge$(on a b)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
(clear c)\\
}%
}

(on b c)$\wedge$(on a b)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
(clear b)\\
}%
}

(on b c)$\wedge$(on a b)\\
(stack b c)\\
(clear b)$\wedge$(clear c)$\wedge$(AE )\\
}%
}

(on b c)$\wedge$(on a b)\\
(stack b c)\\
}%
}

(on b c)$\wedge$(on a b)\\
}%
}

}%
}

(on b c)$\wedge$(on a b)\\
(on b c)\\
}%
}

(on b c)$\wedge$(on a b)\\
(on b c)\\
(stack a b)\\
(clear a)$\wedge$(clear b)$\wedge$(AE )\\
(clear a)\\
(clear b)\\
}%
}

(on b c)$\wedge$(on a b)\\
(on b c)\\
(stack a b)\\
(clear a)$\wedge$(clear b)$\wedge$(AE )\\
(clear a)\\
}%
}

(on b c)$\wedge$(on a b)\\
(on b c)\\
(stack a b)\\
(clear a)$\wedge$(clear b)$\wedge$(AE )\\
}%
}

(on b c)$\wedge$(on a b)\\
(on b c)\\
(stack a b)\\
}%
}

(on b c)$\wedge$(on a b)\\
(on b c)\\
}%
}

(on b c)$\wedge$(on a b)\\
}%
}

}%
}

}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(on c b)\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(on c b)\\
(stack b a)\\
(clear b)$\wedge$(clear a)$\wedge$(AE )\\
(clear b)\\
(clear a)\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(on c b)\\
(stack b a)\\
(clear b)$\wedge$(clear a)$\wedge$(AE )\\
(clear b)\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(on c b)\\
(stack b a)\\
(clear b)$\wedge$(clear a)$\wedge$(AE )\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(on c b)\\
(stack b a)\\
(clear b)$\wedge$(clear a)$\wedge$(AE )\\
(unstack a b)\\
(on a b)$\wedge$(clear a)$\wedge$(AE )\\
(on a b)\\
(clear a)\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(on c b)\\
(stack b a)\\
(clear b)$\wedge$(clear a)$\wedge$(AE )\\
(unstack a b)\\
(on a b)$\wedge$(clear a)$\wedge$(AE )\\
(on a b)\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(on c b)\\
(stack b a)\\
(clear b)$\wedge$(clear a)$\wedge$(AE )\\
(unstack a b)\\
(on a b)$\wedge$(clear a)$\wedge$(AE )\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(on c b)\\
(stack b a)\\
(clear b)$\wedge$(clear a)$\wedge$(AE )\\
(unstack a b)\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(on c b)\\
(stack b a)\\
(clear b)$\wedge$(clear a)$\wedge$(AE )\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(on c b)\\
(stack b a)\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(on c b)\\
(stack b a)\\
(clear b)$\wedge$(clear a)$\wedge$(AE )\\
(clear b)\\
(clear a)\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(on c b)\\
(stack b a)\\
(clear b)$\wedge$(clear a)$\wedge$(AE )\\
(clear b)\\
(clear a)\\
(putdown a)\\
(hold a)\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(on c b)\\
(stack b a)\\
(clear b)$\wedge$(clear a)$\wedge$(AE )\\
(clear b)\\
(clear a)\\
(putdown a)\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(on c b)\\
(stack b a)\\
(clear b)$\wedge$(clear a)$\wedge$(AE )\\
(clear b)\\
(clear a)\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(on c b)\\
(stack b a)\\
(clear b)$\wedge$(clear a)$\wedge$(AE )\\
(clear b)\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(on c b)\\
(stack b a)\\
(clear b)$\wedge$(clear a)$\wedge$(AE )\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(on c b)\\
(stack b a)\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(on c b)\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(stack c b)\\
(clear c)$\wedge$(clear b)$\wedge$(AE )\\
(clear c)\\
(clear b)\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(stack c b)\\
(clear c)$\wedge$(clear b)$\wedge$(AE )\\
(clear c)\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(stack c b)\\
(clear c)$\wedge$(clear b)$\wedge$(AE )\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
(stack c b)\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
(ontable a)\\
}%
}

(ontable a)$\wedge$(on c b)$\wedge$(on b a)\\
}%
}

}%
}

